# Bluntly.ph backend — runtime dependencies (M0 Foundations)
fastapi>=0.115,<0.116
uvicorn[standard]>=0.32,<0.35
sqlalchemy>=2.0,<2.1
alembic>=1.13,<1.15
psycopg[binary]>=3.2,<3.3
pydantic>=2.9,<3.0
pydantic-settings>=2.5,<3.0
pyjwt[crypto]>=2.9,<2.11
cryptography>=43.0
httpx>=0.27,<0.29
redis>=5.0,<6.0
celery>=5.4,<5.6
python-multipart>=0.0.12
supabase>=2.9,<3.0
argon2-cffi>=23.1
anthropic>=0.40
email-validator>=2.2
# Contract term arithmetic (M3 slice 10): relativedelta clamps month-end dates
# correctly (Jan 31 + 1 month -> Feb 28/29). Present transitively via celery, but
# contracts depend on it directly — declare it so a celery change can't drop it.
python-dateutil>=2.9
